// Check if the current URL matches the desired pattern
if (window.location.hostname === 'netcord.in' && window.location.pathname === '/blog') {
    // Modify the URL and redirect
    window.location.href = 'https://netcord.in';
  }
  